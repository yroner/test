const button = document.getElementById("home_btn");

button.addEventListener('click', () => {
    // Переход на другую страницу
    window.location.href = '../index.html'; // Замените на URL вашей страницы
});